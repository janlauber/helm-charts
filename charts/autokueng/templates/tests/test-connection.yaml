apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "autokueng.fullname" . }}-test-connection"
  labels:
    {{- include "autokueng.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "autokueng.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
